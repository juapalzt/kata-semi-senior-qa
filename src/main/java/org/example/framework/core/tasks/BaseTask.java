package org.example.framework.core.tasks;

/**
 * Base abstraction for Screenplay Tasks. Tasks should be small composable units of work.
 */
public interface BaseTask {
    // marker interface for tasks; concrete tasks will define their own methods
}
