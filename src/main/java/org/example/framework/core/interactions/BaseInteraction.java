package org.example.framework.core.interactions;

/**
 * Base abstraction for low-level interactions used by tasks (clicks, types, waits).
 */
public interface BaseInteraction {
    // low-level interaction contract
}
